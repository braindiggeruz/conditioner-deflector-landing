# =========================================================================
# Cloudflare Pages — Environment Variables (set in dashboard, NEVER commit)
# =========================================================================

# --- Server-side (Cloudflare Pages Function /api/lead) ---
# BUYO CPA Network
BUYO_API_URL="https://api.buyo.network/api/v1/leads"
BUYO_API_KEY=""
BUYO_FLOW_ID=""

# Meta Conversions API (server-side Lead event)
META_PIXEL_ID=""
META_CAPI_ACCESS_TOKEN=""
META_TEST_EVENT_CODE=""
META_GRAPH_VERSION="v23.0"

# --- Frontend-safe (Vite, prefixed VITE_, baked into bundle) ---
# Meta Pixel (browser-side) — must equal META_PIXEL_ID
VITE_META_PIXEL_ID=""

# Instant-contact buttons (hidden if empty)
VITE_TELEGRAM_URL=""
VITE_PHONE_NUMBER=""
